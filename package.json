{
  "name": "singlepage_todo_app",
  "version": "1.0.0",
  "description": "It is a single page todo app to check ajax",
  "main": "app.js",
  "scripts": {
    "compile-sass": "node-sass --include-path sass ./public/sass/main.scss ./public/stylesheets/style.css",
    "watch-css": "nodemon -e sass -x \"npm run compile-sass\""
  },
  "author": "Parshant Dhall",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.0",
    "mongoose": "^5.5.10"
  },
  "devDependencies": {
    "node-sass": "^4.12.0"
  }
}
